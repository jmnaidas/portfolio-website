import { Contributions } from '@/components/ui/contributions';
import { work } from '@/data/portfolio';
import { Reveal } from '@/components/ui/reveal';
export function SelectedWork() {
  return <section id="work" className="section container"><Reveal><div className="section-heading"><div><p className="eyebrow">02 / SELECTED WORK</p><h2>Built for the<br /><span className="muted">real world.</span></h2></div><p>A selection of enterprise systems and platforms I’ve helped design, build, and support.</p></div></Reveal><div className="work-grid">{work.map((project, i) => <Reveal key={project.slug} className={`work-card ${i === 0 ? 'featured' : ''}`} delay={i * 0.04}><article id={project.slug}>{i === 0 && <div className="work-art" aria-hidden="true"><div className="art-grid" /><div className="orbit orbit-one" /><div className="orbit orbit-two" /><div className="orbit orbit-three" /><div className="art-core"><span /> <span /> <span /></div><span className="art-label">SIGNALS INTO UNDERSTANDING</span><span className="art-corner">↗</span></div>}<div className="work-content"><div className="work-meta"><span>{project.category}</span><span>{project.number}</span></div><h3>{project.title}</h3><p>{project.summary}</p>{project.role && <p className="work-role">{project.role}</p>}<div className="tags">{project.technologies.map(technology => <span key={technology}>{technology}</span>)}</div><Contributions slug={project.slug} title={project.title} items={project.contributions} /></div></article></Reveal>)}</div><p className="confidentiality">Professional work, presented in context. Project details are intentionally anonymized.</p></section>;
}
